.DS_Store
yarn.lock
app/sculptgl.js
app/index.html
standalone/*
cache/*
node_modules
*~
.idea/*
*.sublime-workspace
*.log
*.map
*.hdr
tools/*.png
tools/dist

# Build output (regenerated by `vite build` / deploy.sh; rsynced to server, not needed in git)
dist/

# Deployment
.last_deployed*
deploy*.sh

# Internal / Local
mac_proxy.py
walkthrough.md
ngrok*
*.sublime-project
cert.pem
key.pem

# Rust build artifacts
voxel_wasm/target/
voxel_wasm/vendor/**/target/

# Claude Code local config
.claude/

# Archived autoremesher (QuadCover) research — keep the small source/docs/patches,
# ignore the multi-hundred-MB regenerable build tree + binaries.
docs/autoremesher_wasm/geogram_wasm_build/
docs/autoremesher_wasm/libgeogram.a
docs/autoremesher_wasm/wasm_build/autoremesher.js
docs/autoremesher_wasm/wasm_build/autoremesher.wasm
docs/autoremesher_wasm/wasm_build/test_ar.*

# Generated by the scratchpad test harnesses (rebuilt on every run)
scratchpad/_*_gen.mjs
