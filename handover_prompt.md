# Handover Prompt

**Project Status**: Finished the `feature/performance-wireframe` branch and the Voxel Polish cycle, deploying v0.9.250 to both Beta and Production environments. We've successfully completed the Voxel Cube Brush (including VR controller orientation), optimized voxel normals by moving them to the worker thread, fixed the "Wireframe Rebuild Paradox" on standalone headsets, and implemented a cache-busting Version Update Prompt (desktop banner + VR HUD logger) to keep users on the latest build.
**Current Working Directory**: `/Users/mattestela/.gemini/jetski/scratch/sculptxr/`

## Recent Work & Context
1. **Voxel Cube Brush & Orientation**: Added a native Cube SDF brush shape alongside the Sphere. Implemented `alignToController` logic allowing the voxel cube to rotate physically with the user's wrist in VR, complete with perfectly mirrored symmetry math and dynamically swapping visual VR cursors.
2. **Voxel Optimization**: Stripped heavy `_computeNormals` and `color` sanitization loops from the main thread (`SculptVoxel.js`) and offloaded them entirely to the Web Worker (`VoxelState.js`).
3. **Wireframe Performance & Quads**: Fixed severe CPU lockups on standalone VR by curing the wireframe rebuild paradox in `MeshStatic.js` and implementing dynamic `MAX_TRIANGLES` limits to intelligently sub-sample lines for dense geometries.
4. **Version Update Prompt**: Built a lightweight polling system in `SculptGL.js` that checks for a new `version.json` payload generated by the deployment scripts. Flags a new update via a clean desktop banner and a non-intrusive 20px warning in the VR Mini-HUD.
5. **Current Build**: Deployed to `tokeru.com/sculptxr/` main production as version `v0.9.250`. Documentation (`todo.md`, `README.md`, `releases.md`) is completely updated.

## Next Steps
The environment is clean and stable. You are starting from a fresh chat context. You can proceed to test the site, or immediately start tackling the next major `todo.md` items:
1. **Dynamic Topology Performance**: Dyntopo still struggles severely on standalone hardware. Now that wireframe is fixed, look into deep profiling Dyntopo vertex splitting on Quest.
2. **Advanced Voxel Tools**: Port more standard tools (Smooth, Move) over to the Voxel brush palette, or begin investigating "straight line" or "曲线/Tubes" voxel modes.
3. **VR Movement Tracking / Ski Navigation**: Look into making world scaling and translation more robust. The user notes an issue with accidental double-grips stopping the flow.